# AutoScraper. python 3.11. JS 렌더 없음 -> 리플레이된 정적 스냅샷 위에서만 돈다.
autoscraper>=1.1.14

# beautifulsoup4 를 4.12 로 못 박는다. 올리면 안 된다.
#   autoscraper 의 _get_valid_attrs 는 style 속성이 없는 요소에도 {'style': ''} 를 붙여
#   규칙을 만든다. bs4 4.13+ 는 빈 문자열을 '속성 없음'과 더는 같게 보지 않아서
#   findAll(tag, {'style': ''}) 이 자기 자신조차 못 찾는다.
#   그 결과 _build_stack 이 조상 체인을 하나도 못 쌓고(규칙 길이 1),
#   get_result_similar 가 **항상 빈 목록**을 돌려준다. 3줄짜리 <li> 목록에서도 실패한다.
#   4.15 에서 실측 확인(2026-09-09). 이 상태로 벤치마크를 돌리면 도구가 아니라
#   내 의존성 해석을 재게 된다.
beautifulsoup4==4.12.3

requests>=2.31
lxml>=5.2
